import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["img.freepik.com", "s3-alpha-sig.figma.com", "img.icons8.com", "imagedelivery.net", "images.pexels.com", "cdn.pixabay.com"], 
  },
};

export default nextConfig;
